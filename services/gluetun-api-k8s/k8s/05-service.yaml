apiVersion: v1
kind: Service
metadata:
  name: gluetun-k8s-api
  namespace: gluetun-system
  labels:
    app: gluetun-k8s-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
  selector:
    app: gluetun-k8s-api

